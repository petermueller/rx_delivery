<h1>Add Location for <%= @pharmacy.name %> </h1>

<%= render "form.html", Map.put(assigns, :action, Routes.pharmacy_location_path(@conn, :create, @pharmacy.id)) %>

<span><%= link "Back", to: Routes.pharmacy_path(@conn, :show, @pharmacy) %></span>
